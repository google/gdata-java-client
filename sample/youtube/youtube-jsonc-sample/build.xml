<?xml version="1.0"?>

<project basedir="." default="compile" name="gdata">

	<property environment="env" />
	<property file="build.properties" />
	<property name="classes.dir" value="${build.dir}/classes" />
	<property name="lib.dir" value="${gdata.dir}/lib" />
	<property name="gdata.jar" value="${gdata.dir}/build-bin/dist/gdata-full.jar" />

	<target name="compile" description="Compile">
		<mkdir dir="${classes.dir}" />
		<javac debug="true" destdir="${classes.dir}" source="1.5" target="1.5">
			<compilerarg value="-Xlint:all" />
			<src path="${src.dir}" />
			<classpath>
				<pathelement location="${gdata.jar}" />
				<pathelement location="${lib.dir}/jackson-core-asl-1.4.3.jar" />
			</classpath>
		</javac>
	</target>

	<target name="run" description="Runs the sample." depends="compile">
		<java classname="com.google.api.data.sample.youtube.YouTubeBasicJsoncSample">
			<classpath>
				<pathelement location="${classes.dir}" />
				<pathelement location="${gdata.jar}" />
				<pathelement location="${lib.dir}/jackson-core-asl-1.4.3.jar" />
				<pathelement location="${lib.dir}/httpclient-4.0.1.jar" />
				<pathelement location="${lib.dir}/httpcore-4.0.1.jar" />
				<pathelement location="${lib.dir}/commons-logging-1.1.1.jar" />
			</classpath>
		</java>
	</target>

	<target name="clean" description="Remove generated files.">
		<delete dir="${build.dir}" />
	</target>

</project>
